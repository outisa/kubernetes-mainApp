apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: mainapp-namespace
  name: hashgenerator-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashgenerator
  template:
    metadata:
      labels:
        app: hashgenerator
    spec:
      volumes:
        - name: shared-logs
          persistentVolumeClaim:
            claimName: logs-claim
      containers:
        - name: main-app-finder
          image: outisa/main-application-finder:1be380fd282adc43a763742a15e9b4ec5e52ade9
          env:
            - name: MESSAGE
              valueFrom:
                configMapKeyRef:
                  name: hashgenerator-cm
                  key: MESSAGE
          volumeMounts:
          - name: shared-logs          
            mountPath: /app/files
        - name: main-app-generator
          image: outisa/main-application-generator:1be380fd282adc43a763742a15e9b4ec5e52ade9
          volumeMounts:
            - name: shared-logs
              mountPath: /app/files

