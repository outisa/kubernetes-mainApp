apiVersion: apps/v1
kind: Deployment
metadata:
  name: hashgenerator-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashgenerator
  template:
    metadata:
      labels:
        app: hashgenerator
    spec:
      volumes:
        - name: shared-logs
          persistentVolumeClaim:
            claimName: logs-claim
        - name: shared-pongs
          persistentVolumeClaim:
            claimName: pongs-claim
      containers:
        - name: main-app-finder
          image: outisa/main-application-finder:928680874e982661ac6ba79d7e3f0a78fc0eab84
          volumeMounts:
          - name: shared-logs          
            mountPath: /app/files
          - name: shared-pongs
            mountPath: /app/pongs
        - name: main-app-generator
          image: outisa/main-application-generator:928680874e982661ac6ba79d7e3f0a78fc0eab84
          volumeMounts:
            - name: shared-logs
              mountPath: /app/files

