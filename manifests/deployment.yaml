apiVersion: apps/v1
kind: Deployment
metadata:
  name: hashgenerator-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hashgenerator
  template:
    metadata:
      labels:
        app: hashgenerator
    spec:
      volumes:
        - name: shared-logs
          persistentVolumeClaim:
            claimName: logs-claim
      containers:
        - name: main-app-finder
          image: outisa/main-application-finder:b217116a1c215920d7f4fc52efca1eca83bd304b
          env:
            - name: MESSAGE
              valueFrom:
                configMapKeyRef:
                  name: hashgenerator-cm
                  key: MESSAGE
          volumeMounts:
          - name: shared-logs          
            mountPath: /app/files
        - name: main-app-generator
          image: outisa/main-application-generator:b217116a1c215920d7f4fc52efca1eca83bd304b
          volumeMounts:
            - name: shared-logs
              mountPath: /app/files

